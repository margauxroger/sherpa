console.log('hello')
console.log('<%= @ua.session_id %>')
console.log('<%= @ua.flashcard_id %>')


deckElement = document.querySelector('.stackedcards-container');
console.log(deckElement);


flashcardHTML = `
        <div class="card-item no-show">
            <div class="d-flex flex-column align-items-center front-flashcard">
            <!-- <img class="icon-question-flashcard mt-5" src="https://picsum.photos/201/300" /> -->
            <div  class="question-flashcard mt-5 mb-4 text-center">
              <h3><%= @flashcard.question %></h3>
            </div>
            <form class='flashcard-form'>
              <textarea class="user_input" placeholder="Enter your answer here"></textarea>
            </form>
            <label for="card1" class="button-submit-flashcard mt-5" aria-hidden="true" >
                Check Answer
            </label>
            <div class="hidden-answer mt-5 no-show">
              <p><%= @flashcard.answer %></p>
            </div>

          <div class="global-actions mt-5">
            <%= link_to students_session_user_answers_path(session_id: @session.id, flashcard_id: @flashcard.id, output: false), method: 'post', remote: true, class: "left-action" do %>
              🥴
            <% end %>
            <div class="top-action" style="display:none;">Top</div>
            <%= link_to students_session_user_answers_path(session_id: @session.id, flashcard_id: @flashcard.id, output: true), method: 'post', remote: true, class: "right-action" do %>
              😎
            <% end %>
          </div>
          </div>
        </div>
        `
<% unless @ua.output %>
deckElement.insertAdjacentHTML('beforeend', flashcardHTML);
<% end %>

obj = document.getElementById('stacked-cards-block');

stackedCardsObj = obj.querySelector('.stackedcards-container');
listElNodesObj = stackedCardsObj.children;

function countElements() {
      maxElements = listElNodesObj.length;
    };

countElements();
console.log(maxElements)

console.log('I am here')


