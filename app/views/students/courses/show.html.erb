


<% @course.material.chapters.each do |chapter| %>
<%= link_to chapter.name, students_sessions_path(chapter_id: chapter.id), method: :post, class: "material-card ml-3" %>
<% end %>





<%= render 'shared/navbar_mobile_bottom', course: @course %>

